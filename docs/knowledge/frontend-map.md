# 前端地图(frontend/src/)

React(Vite + Cloudscape Design)前端逐目录/模块职责。给 AI 协作者:找「某 UI 住哪」先查这里。
权威真相源(api-client / storage / toast / AgentModelPicker)见根 `CLAUDE.md`,不在此重复。
**UI 铁律**:不要 emoji / 装饰图标(状态用文字/边框/高亮,图标用 Cloudscape `iconName`);加设置项前先 grep 后端有没有真读那个 pref。

## 构建与入口

- **多入口**:`entries/`——`platform.jsx`(平台主 SPA)、`game-console.jsx`(游戏台)、`tavern.jsx`(酒馆)、`login.jsx`。对应根 HTML(`Platform.html`/`Game Console.html`/`Tavern.html`/`Login.html`)。Vite 配置见 `frontend/vite.config.js`。
- `router.js` — Platform SPA 轻量 History 路由(`plNavigate(id)` + `pl-navigate` 事件,干净 URL)。
- **顶层 App 壳**:`platform-app.jsx`、`game-app.jsx`、`tavern-app.jsx`、`login-app.jsx`(**已拆薄**,自包含件/展示件住 `components/login/`,见 §组件目录)。

## 核心通信/工具(必用,别自造)

- `api-client.js` — **后端 API 单一入口**,装 `window.api.<group>.<method>()`,同源 Cookie 会话 + SSE helper(`/api/chat`、`/api/opening`),离线回退 MOCK。
- `lib/storage.js` — localStorage 封装(`lsGet`/`lsSet`/`lsGetJSON`/`lsSetJSON`/`lsRemove`)。`lib/prefs.js` — 偏好读写。`lib/creds.js`/`lib/crypto-safe.js` — 凭据/加密。
- `toast.jsx` — **双 toast 总线**:`createToastChannel` + `setWindowToast`(平台 `window.toast`)/ `setApiToast`(游戏 `window.__apiToast`)。两条独立总线是刻意的,别合并、别自己 mount stack。
- `data-loader.js`、`state-event-bridge.js`、`runtime-telemetry.js`/`web-vitals-rum.js`(埋点)、`i18n/`(`index.js` + `locales/en.json`、`zh-CN.json`)。

## 页面(pages/)

整页视图:`admin.jsx`(**已拆薄为转发壳**,实现住 `components/admin/`)、`cards.jsx`(**已拆薄为路由壳**,实现住 `components/cards/`)、`saves.jsx`(**已拆薄为路由壳**,实现住 `components/saves/`)、`feedback.jsx`、`device.jsx`、`rath.jsx`(RATH 观测台)、`tavern.jsx`、`md-editor.jsx`(**已拆薄为转发壳**,三栏 IDE 剧本编辑器实现住 `components/md-editor/`)、`script-review.jsx`、`script-edit-canon.jsx`/`script-edit-worldbook.jsx`、`script-modules-panel.jsx`。

- **[已拆分]** `settings.jsx`(4093→131)只保留 `SettingsPage` 壳+转发 export;实现住 `components/settings/`(shared/pref-sections/models-section+其二次拆分 models-catalog·model-list·model-modals·provider-config/modelparams/module-models/memory/perm/deploy/account/danger)。
- **[已拆分]** `scripts.jsx`(3680→128)只保留 `ScriptsPage` 路由壳+12 具名 export 转发;实现住 `components/scripts/`(shared/ScriptDetail/ScriptsList/ScriptsImport——后三个已**二次拆分**为主组件壳 + 同目录子文件,见下)。
- **[已拆分]** `admin.jsx` 只保留 14 个管理页组件的具名 export 转发(供 `platform-app.jsx` 再转发给 `entries/platform.jsx`);实现全部住 `components/admin/`(见下)。
- **[已拆分]** `saves.jsx` 只保留 `SavesPage` 路由壳 + 具名 export 转发(`SavesPage`/`SavesListView`/`BranchesPage`/`ContinuePicker`/`NewGameModal`);实现住 `components/saves/`(见下)。
- **[已拆分]** `cards.jsx` 只保留 `CardsPage` 路由壳 + 具名 export 转发(共 12 个:`CardsPage`/`CardGrid`/`UserCardsView`/`NpcCardsView`/`CardEditModal`/`TavernImportModal`/`CardSheet`/`cardSnippet`/`CardEditFields`/`cardFormInit`/`cardFormPayload`/`npcToUserCardBody`);实现住 `components/cards/`(见下)。外部引用面广(`entries/platform`、`tavern-drawer`/`pages/tavern`/`tavern-app`、`components/scripts/ScriptDetail`、`components/saves/NewGame`、`mobile/pages/MobileCards`),转发壳保持这些 import 不变。
- **[已拆分]** `md-editor.jsx` 只保留 12 行转发壳:`import './md-editor.css'`(模块级副作用留壳)+ `export default MdEditorPage`(默认导出供 `entries/platform.jsx` lazy 边界不变)+ `export { nodeKey }` 具名转发;实现住 `components/md-editor/`(见下)。
- **[已拆分]** `script-edit-canon.jsx`(1176→7 行壳)只保留两个编辑视图的具名 export 转发(`CanonEntityEditorView`/`AnchorEditorView`,`components/scripts/ScriptDetail.jsx` 从原路径 `../../pages/script-edit-canon.jsx` import 不变);两个天然独立编辑视图纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到 `components/script-edit/`(见下)。姊妹页 `script-edit-worldbook.jsx` 单视图不动。

## 组件(components/)

- `components/platform/` — 平台外壳页:`AuthPage.jsx`、`LibraryPage.jsx`、`ModulesPage.jsx`、`ProfilePage.jsx`、`UsagePage.jsx`、`CapPages.jsx`、`UnifiedSearch.jsx`、`DialogHost.jsx`、`WelcomeModal.jsx`、`achievements.jsx`、`me-pages.jsx`(**已二次拆分为转发壳**,见下)、`shared.jsx`。
  - **[已二次拆分]** `me-pages.jsx`(1078→13 行壳)只保留 `MePage` 主组件(按 `subPage` 分发 overview/edit/settings)+ 转发 export(`platform-app.jsx` 只引用 `MePage`,引用面不变);三个页面级组件纯机械搬家(逐字节等价、零行为变化)到同目录:`MeOverview.jsx`(概览 Hero/统计/成就墙/最近活动 + `ME_ACTIVITY` mock 常量,唯一读者;引用 `achievements.jsx` 的 `flushAchievementToasts` 会话内去重单例,import 关系保持不破坏 dedup Set)、`MeEditProfile.jsx`(编辑资料:头像/基本资料/联系方式/本地化)、`MeUserSettings.jsx`(隐私/数据共享/账号安全/通知/数据所有权)。无 Python/JS 守卫 read_text 引用本文件。
- `components/settings/` — 设置分区(settings.jsx 拆分目标):`account-section`、`models-section`/`module-models-section`/`modelparams-section`、`memory-section`、`perm-section`、`deploy-section`、`danger-section`、`pref-sections`。
  - **[已二次拆分]** `models-section.jsx` 只保留主组件 `ModelsSection`(≈476 行)+ 全量 export 转发壳(对外 13 具名导出不变:`ModelsSection`/`ApiModelsList`/`AddModelModal`/`EditApiModal`/`ValidateModal`/`VisibilityModal`/`ProviderCard`/`ProviderConfigSection`/`ModelNameCell`/`HealthDot`/`MODELS_DATA`/`PROVIDERS_CONFIG`/`normalizeApiId`,供 `pages/settings.jsx` 再转发给 `ModelConfigInterceptModal` 等)。自包含子树纯机械搬家(逐字节等价、零行为变化)到同目录:`models-catalog.js`(`MODELS_DATA`/`PROVIDERS_CONFIG` + ID 归一化别名 `normalizeApiId`/`credentialApiIdForCatalog`/`catalogApiIdForCredential` + 格式化 `sourceLabel`/`fmtCtx`/`fmtPrice`)、`model-list.jsx`(`ApiDetailPanel` + `ApiModelsList` + `ModelNameCell` + `StatusDetailBadge` + `HealthDot`)、`model-modals.jsx`(`AddModelModal`/`EditApiModal`/`VisibilityModal`/`ValidateModal`;`VisibilityModal` 内 `getCaps` 保持模块级自由引用=全局 `window.getCaps`,勿 import)、`provider-config.jsx`(`ProviderConfigSection` + `ProviderCard`)。回引 `import { CAP_LABEL } from '../../pages/settings.jsx'`(render 时才读)随 `model-list`/`model-modals` 保持既有循环模式。守卫 `test_models_sync_base_url_override.py`/`test_no_button_in_button.py` 断言目标(`base_url: cred.base_url_override || api.base_url`、`<span onClick=...stopPropagation...><SettingsToggle`)仍在 `ModelsSection` 内 → read_text 路径无需改;button-in-button 通扫为动态 rglob,自动跟随 `<SettingsToggle` 分散到 `model-list.jsx`。
- `components/admin/` — 后台管理分页(admin.jsx 拆分目标):`shared.jsx`(`fmtTime` 工具)、`users-section`、`usage-section`、`audit-section`、`health-section`、`logs-section`、`registration-section`、`security-section`、`dmca-sections`(下架队列 + Strike 两页)、`csam-section`、`aup-section`、`maintenance-section`、`feedback-section`、`achievements-section`。
- `components/scripts/` — 剧本相关三大页(scripts.jsx 拆分目标),各自**已二次拆分**为「主组件壳 + 同目录子文件」(纯机械搬家、逐字节等价、零 DOM/视觉/行为变化;外部 import 面零改动——`pages/scripts.jsx` 仍从三主文件 import + 转发,`ScriptsList.jsx` 仍从 `ScriptDetail.jsx` import `ScriptDetailPanel`、从 `ScriptsImport.jsx` import `ScriptsImportView`)。`shared.js`(`scriptPlayBlockReason`/`activeJobPlayBlockReason`/`SPLIT_RULES`/`IMPORT_JOB_TERMINAL_STATUSES` 等公共常量/纯工具,三主文件共用)不动。
  - **[已二次拆分]** `ScriptsImport.jsx`(1360→1091)只保留主组件 `ScriptsImportView`(仍是单体大组件——内部全是闭包纠缠 handler,无独立具名子定义可拆,宁保守不肢解)+ 私有 helper(`isExpiredUploadError`/`isCredentialsRequiredError` 别名/`PENDING_IMPORT_*` 键/`IMPORT_STAGES`——唯一读者是 `ScriptsImportView` 故按「常量与唯一读者同居」留守)+ 转发 export(`ScriptsImportView`/`ImportJobBanner`/`ImportJobResult`/`ImportEstimateView` 对外不变)。三个 SSE 展示视图搬到 `import-job-views.jsx`(`ImportJobBanner` 进度横幅 + `ImportJobResult` 结果 Alert + `ImportEstimateView` 预算视图)。**守卫 `test_canon_estimate_and_progress.py` read_text 断言 `IMPORT_STAGES` extract 阶段 `tok_per_chap>=2000`;因 `IMPORT_STAGES` 未搬走(随唯一读者留守),该守卫仍读 `ScriptsImport.jsx`,路径无需改。**
  - **[已二次拆分]** `ScriptDetail.jsx`(1240→684)只保留主组件 `ScriptDetailPanel` + 转发 export(`ScriptDetailPanel`/`KbExtractPanel` 对外不变);四子组件各归一文件:`VersionHistoryDrawer.jsx`(版本历史 Drawer)、`SharingModeSelector.jsx`(共享模式选择器)、`CoverFrame.jsx`(自适应封面)、`KbExtractPanel.jsx`(LLM 知识提取面板 + `_EXTRACT_STAGE_LABEL_KEYS`/`_stageIndicator` 唯一读者同居)。autosave/健康度病灶 handler 逐字保留在 `ScriptDetailPanel` 内。
  - **[已二次拆分]** `ScriptsList.jsx`(954→521)只保留主组件 `ScriptsListView` + 转发 export(`ScriptsListView`/`ScriptsLibraryView`/`ChaptersModal`/`OverridesModal` 对外不变);三组件各归一文件:`ScriptsLibraryView.jsx`(在线剧本库)、`OverridesModal.jsx`(overrides JSONB 编辑弹窗)、`ChaptersModal.jsx`(章节浏览/重命名/合并/拆分/重切弹窗,`SPLIT_RULES` 随迁)。
- `components/saves/` — 存档相关(saves.jsx 拆分目标):`SavesList.jsx`(`SavesListView` + 就地设置表单/分支节点列表/导出弹窗)、`Branches.jsx`(`BranchesPage` git-graph 页 + `ContinuePicker` 继续游戏选择器,守卫测试 read_text 断言此文件)、`NewGame.jsx`(**已二次拆分为转发壳**,见下)。
  - **[已二次拆分]** `NewGame.jsx`(1303→4 行壳)只 `export { NewGameModal } from './NewGameModal.jsx'`;实现纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同目录:`NewGameModal.jsx`(`NewGameModal` 主向导 + `newGame*` 就绪判定纯工具 + `NEWGAME_*` 常量——唯一读者故同居,import `BirthpointStep`/`IdentityStep`)、`BirthpointStep.jsx`(Step 3 出生点选择,**进度信号病灶 UI 侧,逐字复制**;附 mock 出生点数据 / `WizardProgress` / `InlineErr` 展示件,`InlineErr` 为其唯一读者)、`IdentityStep.jsx`(Step 4 初始身份:本体来源 + 身份卡 overlay + 是否知情)。外部只引用 `NewGameModal`(`pages/saves.jsx`→`platform-app.jsx`),转发壳保持其 import 面不变;无 Python/JS 守卫 read_text 引用本文件(saves/ 守卫只断言 `Branches.jsx`)。
- `components/cards/` — 角色卡相关(cards.jsx 拆分目标,纯机械搬家零行为变化):`helpers.js`(纯工具 + mock 数据:`cardFormInit`/`cardFormPayload`/`npcToUserCardBody`/`cardSnippet`/`_oneLine`/`clampLines`/`ELLIPSIS_1`/`USER_CARDS`/`NPC_CARDS`,被 mobile/tavern/saves/scripts 借用)、`CardFields.jsx`(`CardEditFields` 共享字段组 + `CardSheet` 只读档 + `SkillContentSection` 人格 skill 折叠拉取)、`CardGrid.jsx`(`CardGrid` 卡面网格 + `promoteNpcToUserCard` NPC→用户卡迁移,历史病灶)、`CardDetailPanel.jsx`(`CardDetailPanel` 详情面板 + `PersonaImageGallery`/`PersonaThumbStrip` 人设图,含 `CardAv`/头像刷新逻辑)、`TavernImportModal.jsx`(酒馆卡/聊天导入弹窗)、`CardEditModal.jsx`(EC2 式全屏编辑器 + 克隆/转卡流程)、`CardViews.jsx`(`UserCardsView`/`NpcCardsView`/`OnlineCardsView` 三列表视图)。
- `components/script-edit/` — 剧本编辑器两视图(script-edit-canon.jsx 拆分目标,纯机械搬家零行为变化):`helpers.js`(共享纯工具 `snippet`,两视图共用)、`CanonEntityEditorView.jsx`(canon 实体行内表格编辑器 + 私有件 `AddAliasInput`/`typeBadgeColor` + 常量 `ENTITY_TYPES`/`IMPORTANCE_OPTIONS`)、`AnchorEditorView.jsx`(story anchor 行内表格编辑器 + 常量 `STORY_PHASES`)。`DetailDrawer` import 路径随迁改为 `../DetailDrawer.jsx`;死 import `CSContainer` 顺手去掉(原文件两视图均未用);无 Python/JS 守卫 read_text 引用这些文件。
- `components/tavern-page/` — 从 `pages/tavern.jsx` 拆出的自包含件(纯机械搬家零行为变化):`TavernCharacterSelect.jsx`(「选择角色」面板,点卡即建对话进聊天;tavern.jsx 内部唯一读者,默认导出)。**tavern.jsx 主体仍是单体 `TavernPage`(~1050 行,经 `hooks/useTavernChatRun.js` 收口聊天 run),按 game-console/MobileTavern 前例宁保守不肢解——只搬出这一自包含具名组件;`import './tavern-platform.css'` 模块级副作用留主文件。**
- `components/login/` — 登录页壳(login-app.jsx 拆分目标,纯机械搬家零 DOM/视觉/行为变化):`nextTarget.js`(回跳目标解析纯工具 `__resolveNextOrDefault`,主壳 handler 借用)、`SchemaField.jsx`(schema 驱动的单字段渲染,含 `terms_accepted` 法律链接 label)、`OtpInput.jsx`(6 位验证码输入件)、`InviteScreen.jsx`(`?invite=` 短路轻量注册屏,展示件)、`AuthForms.jsx`(七个 auth-mode `<form>` 展示件:`VerifyForm`/`MagicOtpForm`/`NeedsProfileForm`/`CodeLoginForm`/`ForgotForm`/`ResetForm`/`MainAuthForm`;JSX 逐字节等价,**inline setErr + `pl-auth-error role=alert` 非静默错误 UI 逐字保留**;各展示件内部 `useTranslation()` 取 `t`,`OtpInput`/`SchemaField` 就近 import)。**主壳 `login-app.jsx`(687 行)保留 `LoginApp` 主组件——全部 state + 七个挂载 useEffect(已登录短路/schema 拉取/`#reset` hash/`?invite=`/magic-link 消费/Turnstile 脚本+挂件)+ 全部闭包纠缠 auth handler(`submit`/`handleVerify`/`handleReset`/`requestLoginCode`/`handleMagicOtpVerify`/`handleProfileSubmit`/`handleForgot`/`submitInvite` 等)整体留守——闭包纠缠 + `turnstileSitekey` 派生穿插 effect 与 render 之间,effect 无法机械外提;按 MobileTavern(610)/ScriptsImportView(1091)前例宁保守不肢解(拆得少=合格)。展示件经 props 收口(handler 定义仍在壳内,以 prop 下传),渲染分支条件/章节注释/wrapper/tabs 逐字保留。** 唯一对外 export `LoginApp` 不变(`entries/login.jsx` + `__tests__/login-app.test.jsx` 引用面零改动);无 Python/JS 守卫 read_text 引用本文件。
- `components/md-editor/` — 剧本三栏 IDE(md-editor.jsx 拆分目标,纯机械搬家零行为变化):`helpers.js`(常量 + 无 UI 工具:`IS_MAC`/`NODE_GROUPS`/`api`/`toast`/`stripChapterPrefix`/`canonTypeZh`/`KIND_ICON`/`CAN_*`/`nodeKey`/`kindLabelZh`)、`node-crud.js`(树内实体增删改 `createNode`/`renameNode`/`deleteNode` + 分组列表 `fetchGroupList`)、`node-io.js`(节点内容读写:`loadNodeContentMeta` 加载→toMd、`saveNodeContent` diff-only 保存 + front-matter 冻结闸)、`ContextMenu.jsx`(三处共用右键菜单)、`TbIcon.jsx`(顶栏图标)、`FileTree.jsx`(左栏资源管理器 + `NewMenu`)、`EditorPane.jsx`(中栏编辑器外壳)、`QuickOpen.jsx`(Mod+P)、`GlobalSearch.jsx`(Mod+Shift+F 全书检索)、`ChapterHistory.jsx`(版本历史)、`WritingRules.jsx`(写作规范)、`ProblemsPanel.jsx`(审稿问题面板)、`MdEditorPage.jsx`(页面壳 + 状态编排,默认导出;agent 写库 vs 未保存改动互覆盖 / autosave 竞态相关 handler `openConflictMerge`/`saveTab`/`refreshTab`/`proposeChapterDiff` 原样保留=历史病灶,勿改动)。
- `components/game/` — 游戏台侧栏面板族(game-panels.jsx 拆分目标,每个 tab / section 一文件):`RightPanel.jsx`(`RightPanel` 外壳 + `PANEL_TABS`)、`PanelStatus.jsx`(状态 tab:`_statusProfileFor`/`Module`+`NovelStatusProfile`/`PanelStatus`,守卫 `test_status_panel_content_pack_profile` read_text 断言此文件)、`PanelRules.jsx`(5E 规则 tab,守卫 `test_panel_rules_isolation` read_text 断言此文件)、`PanelMemory.jsx`(记忆 tab,pinned/notes/facts 桶各自独立渲染路径=历史病灶,勿统一)、`PanelWorldbook.jsx`、`PanelCharacters.jsx`(三层人物 + `CharacterCard`)、`PanelTimeline.jsx`(时间线 + `WorldlineAnchorsSection`)、`PanelContext.jsx`(上下文 + DemandLedger)、`PanelDebug.jsx`、`ForcedSetSection.jsx`、`WorldbookSections.jsx`(`WorldbookOverlaySection` + `RegexScriptsSection`,酒馆抽屉/设置/移动共用)、`InlineEditField.jsx`(共用 inline editor)。另有 `game-app.jsx`(游戏台外壳)拆分目标(纯机械搬家零行为变化):`GameLeftRail.jsx`(`LeftRail` + `RunSteps*`/`ThinkingPill`/`RunStateSection`/`RunDetailRail`/`BranchTreeRail`;守卫 `test_branch_graph_head_only_filter`/`test_branch_runtime_switch`/`test_branch_graph_vscode_style` 现读「壳 + components/game 全量拼接」断言 BranchTreeRail)、`GameChatMessages.jsx`(`MsgActions`/`Fork`+`DeleteConfirmModal`/`NarrativeBlock`/`PlayerBlock`/`ChatImageGroup`/`useSaveImages`/`SaveImagesStrip`/`renderNarrativeWithInlineTools`)、`GameChatArea.jsx`(`ChatArea`)、`GameSettingsModal.jsx`(游戏内设置弹窗 + `_read*` 读取器 + `SettingRow`/`SwitchTiny`)、`GameDrawers.jsx`(`HistoryDrawer`/`SearchDrawer`)、`GameTopBar.jsx`(`TopBar`)。**toast 总线 install(`createToastChannel`+`GameToastStack`)顶层副作用留在 game-app.jsx 壳——历史大坑(曾劫持桌面 `__apiToast`),勿搬。** 另有从 `entries/game-console.jsx` 入口拆出的自包含展示组件(纯机械搬家零行为变化):`ScriptVersionSelect.jsx`(顶栏 script 版本切换下拉)、`GCWelcomeModal.jsx`(使用须知弹窗)。**入口的 SSE run-loop/reducer/`on_*` handler 家族(`startRunReal`/`publicStage` 推进/`isCurrentRun` 守卫/软回滚 `on_rewind`)因闭包纠缠整体留守 `entries/game-console.jsx`(~1.75k 行),不拆——拆错=灾难;`PUBLIC_STAGES`/`mapAgentPhase`/`advancePublicStage` 亦留守。** 另有 `game-composer.jsx`(输入台)拆分目标(纯机械搬家零行为变化,逐字节等价):`GameComposerMenus.jsx`(`SLASH_COMMANDS`/`ATTACH_GROUPS` 常量 + `CommandMenu`/`AttachMenu`/`MentionMenu` 浮层菜单)、`GameComposerPopovers.jsx`(`PERMISSION_OPTIONS` 常量 + `ModelPopover`/`EffortSection`/`PermissionPopover`;**`ModelPopover` 的 `source='init'` 回声抑制=历史病灶,与 `AgentModelPicker` 同源,勿改**)、`GameConfirmStrip.jsx`(`ConfirmStrip`/`ConfigCard`/`formatVal`/`isConfigCard` 非阻塞确认条族)、`GameContextUsage.jsx`(`ContextUsage`/`ContextBreakdownPanel` 上下文用量环)、`GameSuggestionRow.jsx`(`SuggestionRow`)。**主组件 `Composer` 与显示助手 `_currentModelLabel` 因与输入台状态闭包纠缠留守 `game-composer.jsx`(壳 405 行);全部具名 export(`Composer`/`ConfirmStrip`/`SuggestionRow`/`MentionMenu`/`SLASH_COMMANDS`/`PERMISSION_OPTIONS`/`ContextUsage`/`ContextBreakdownPanel`)仍从 `game-composer.jsx` 转发,消费者(game-console/tavern/mobile/ProfilePage/MdEditorAgent)import 路径不变。** 守卫 `test_composer_live_data`/`test_api_models_envelope`(integration,读 `game-composer.jsx`,`_currentModelLabel`+`gameState.app.model` 留守故不受影响)。
- **可复用**:`AgentModelPicker.jsx`(**按需 LLM 增强统一用它**)、`Modal.jsx`/`ConfirmDialog.jsx`/`DetailDrawer.jsx`、`DesktopDialogHost.jsx`(桌面裸 confirm/prompt 的抽屉 Host)、`GlobalTaskFloater.jsx`(全局后台任务浮窗)、`HelpDrawer.jsx`(文档 iframe)、`ErrorBoundary.jsx`、`ToolCallBlock.jsx`、`PolicyNoticeBanner.jsx`、`AdultSplash.jsx`、`ModelConfigInterceptModal.jsx`。
- **编辑器/媒体**:`CodeMirrorEditor.jsx`、`MdEditorAgent.jsx`(写作搭档)、`EditorKbPanel.jsx`、`EditorPlaytest.jsx`、`GmStyleEditor.jsx`、`MediaStudio.jsx`/`MediaUploadZone.jsx`/`GenerateImageModal.jsx`/`ImageLightbox.jsx`/`ImageSizePicker.jsx`、`ModuleMatrixOverview.jsx`/`ModuleStatusCard.jsx`、`RebuildEstimateModal.jsx`/`RebuildJobBanner.jsx`、`AcceptanceAbPanel.jsx`(A/B 验收)、`FileLibrary.jsx`、`AvatarImg.jsx`/`CharacterCardHero.jsx`。

## 游戏台(顶层散件)

`game-app.jsx`(**已拆薄为转发壳 + 保留 toast 总线 install 顶层副作用**,组件实现住 `components/game/`)、`game-composer.jsx`(**已拆薄为 `Composer` 主组件 + `_currentModelLabel` + 全量转发 export**,菜单/浮层/确认条/用量环实现住 `components/game/`,见上文 §组件目录)、`game-panels.jsx`(**已拆薄为转发壳**,侧栏面板实现住 `components/game/`)、`game-icons.jsx`、`console-assistant-navigation.jsx`、`branch-graph.jsx`(分支图)、`narrative-strip.js`、`markdown-render.jsx`、`worldbook-status-toast.js`。样式:`game-console.css`、`markdown-render.css`、`tavern.css`、`platform.css`、`media.css`、`motion.css`、`tokens.css`。

## Hooks / UI kit

- `hooks/` — `useTavernChatRun.js`(酒馆聊天回合)、`useImageGeneration.js`、`useStickToBottom.js`。
- `ui/` — `kit.jsx` + `kit.css`(共享 UI 原子)。
- `cloudscape-theme.js`、`responsive.jsx`、`ui-atlas.js`、`agent-modules.js`、`mock-data.js`。

## 移动 Web 壳(frontend/src/mobile/)

**注意:这是 web 前端内的响应式移动壳(`?m2=1` 门控),不是顶层 `mobile/` 那个独立 Expo app。**
`MobileRoot.jsx`(壳)、`MobileHome.jsx`、`chrome.jsx`、`launch.js`、`Composer.jsx`/`Field.jsx`/`Sheet.jsx`/`dialog.jsx`/`icons.jsx`、`game/`(`MobileGame.jsx`、`panels.jsx`)、`pages/`(`MobileSaves`/`MobileScripts`/`MobileCards`/`MobileTavern`/`MobileSettings`/`MobileMe`/`MobileNewGame`/`MobileCaps`/`MobileAdmin`)。

- **[已拆分]** `mobile/pages/MobileAdmin.jsx` 只保留 47 行路由壳(`switch` 按 `nav.page`=`admin-xxx` 分发 + 具名 export `MobileAdmin` / 默认导出,供 `MobileRoot` 消费);15 个 section + 菜单 + sheet 全部纯机械拆到 `mobile/admin/`(逐字节等价、零行为变化):`shared.jsx`(`fmtTime`/`fmtDate`/`LoadingRow`/`ErrRow`/`EmptyRow`)、`sheets.jsx`(`ConfirmSheet`/`InputSheet`——**【移动端铁律·防误合】** `.sheet-sub` 文案样式 + busy 守护(处理中禁 scrim 点关)+ `sheet-actions` marginTop 与桌面/统一版有真实视觉·行为差,绝不与桌面 `Sheet` 合并、绝不收口到 `mobile/Sheet.jsx`,原样保留;细节见 `ConfirmSheet` 上方 GUARD 注释)、`menu.jsx`(`getSections`/`AdminMenu`)、`users`/`usage`/`audit`/`health`/`logs`/`registration`/`security`/`maintenance`/`dmca`(下架队列 + Strike 两页)/`csam`/`aup`/`feedback`/`achievements`/`deploy`.jsx(各一 `Section*`)。**与桌面 `components/admin/` 是两套独立实现(移动端零 Cloudscape/数据走 `window.api.admin.*`),绝不复用其代码。**

**移动端拆分铁律**:大页拆成同级 `mobile/<域>/` 子目录,壳只保留路由 + 具名/默认 export(引用面不变),壳用 `../<域>/` import;**绝不 import 桌面 `components/`/`pages/*.jsx`——移动端自包含,只复用共享 infra(`../../i18n`/`../icons.jsx`/`../../lib/creds.js`/`../../platform-app.jsx`)**。

- **[已拆分]** `pages/MobileScripts.jsx`(255 行壳,原 1795 行)只保留 `MobileScripts` 路由壳(list / import / library 三视图切换,`MobileRoot.jsx` 引用不变:具名 `export function MobileScripts` + `export default`);页面主体纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/scripts/`:`helpers.js`(纯工具/常量 `fmtWan`/`fmtN`/`ACTIVE_STATUSES`/`TERMINAL_STATUSES`/`getSplitRules`/`isPlayBlocked`)、`EmptyState.jsx`(通用空态)、详情七子视图 `ChaptersView.jsx`/`WorldbookView.jsx`/`NpcView.jsx`/`TimelineView.jsx`/`VersionsView.jsx`/`OverridesView.jsx`/`ShareView.jsx`、`ScriptDetailView.jsx`(详情壳 + 子视图路由)、`ImportView.jsx`(导入向导)、`LibraryView.jsx`(在线剧本库)。`nav.toast` 通路(42 处)逐字保留;无 Python/JS 守卫 read_text 引用本文件。
- **[已拆分]** `pages/MobileSettings.jsx`(133 行壳,原 1799 行)同型:只保留 `SECTIONS`/`ROUTE_MAP` + hub 列表/分区 shell 的主组件(`MobileRoot.jsx` 引用不变:具名 `export function MobileSettings` + `export default`);各分区实现纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/settings/`:`shared.jsx`(可复用小件 `Toggle`/`SetGroup`/`MSlider`/`Seg` + 自动保存 hook `usePrefSave` + K/M 格式化 `fmtCtx`)、`pref-section.jsx`(`PrefSection`)、`models-section.jsx`(`ModelsSection` + 供应商详情 `ProviderDetail` + 别名 `normId`/`credId`/`catId`)、`modelparams-section.jsx`(`ModelParamsSection` + `MP_DEFAULTS`/`MP_PRESETS` + `readPref`/`readNumPref` 薄别名)、`module-models-section.jsx`(`ModuleModelsSection` + `FeatureToggleM` + `MODULES`/特性文案默认表)、`memory-section.jsx`(`MemorySection`)、`perm-section.jsx`(`PermissionsSection` + `HIGH_RISK_ALL`/`CUSTOM_WL_RE`)、`account-section.jsx`(`AccountSection`)、`danger-section.jsx`(`DangerSection`)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/settings/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../Field.jsx`/`../../lib/*` 全不变);绝不 import 桌面 `components/settings/`。守卫 `test_models_sync_base_url_override.py` 的 `MOBILE_SETTINGS_JSX`(断言 `base_url: cred.base_url_override || api.base_url`、`base_url_override: c.base_url_override`,已随 `ModelsSection` 搬到 `models-section.jsx`)改读「壳 + `mobile/settings/*.jsx` 全量拼接」,断言不动。
- **[已拆分]** `pages/MobileMe.jsx`(50 行壳,原 1609 行)同型:只保留「我的中心」路由壳(overview / edit / settings / usage / wall 五视图,由 `nav.page` + 内层 `innerNav.go` 切换;`MobileRoot.jsx` 引用不变:具名 `export function MobileMe` + `export default`);页面主体纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/me/`:`helpers.js`(纯工具/常量 `fmtN`/`fmtWan`/`fmtDate`/`fmtAgo`/`ACHV_CAT_ORDER`/`TIER_RANK`/`TIER_COLOR`)、`shared.jsx`(共用小件 `PageHead`/`Toggle`/`SetRow`/`ActionBtn`/`Input`/`Select`/`ConfirmSheet`——**【移动端铁律·防误合】** `ConfirmSheet` 是纯 inline-style 抽屉(scrim `rgba(…,0.6)`≠`.sheet-scrim` 0.5、圆角 20px≠22px、无从底滑入 transform),`SetRow` 是 inline「信息行 + danger 变体」≠ `mobile/Field.jsx` 的竖排 Field / 开关 ToggleRow,二者绝不与 `mobile/Sheet.jsx` / `Field.jsx` / 桌面合并,原样保留;细节见各自上方 GUARD 注释)、`ViewOverview.jsx`(个人主页 Hero+统计+成就摘要+活动)、`ViewEdit.jsx`(编辑资料+头像上传)、`ViewSettings.jsx`(账户设置 + 会话/历史/改密/人格/导出/可见性/隐私政策/停用/删除等子视图)、`ViewUsage.jsx`(用量统计,内含 `USAGE_RANGES`/`BarChart` 本地件)、`ViewWall.jsx`(成就墙,支持他人公开墙)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/me/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../../i18n`/`../../platform-app.jsx`/`../../components/AvatarImg.jsx` 全不变);绝不 import 桌面 `components/`。无 Python/JS 守卫 read_text 引用本文件。
- **[已拆分]** `pages/MobileTavern.jsx`(610 行壳,原 1599 行)只保留 `MobileTavern` 顶层组件(两屏 list/chat 切换 + **与页面状态闭包纠缠的 SSE run-loop 接线**——`startRun`/`onRetry`/`openChat`/`doRename`/`doArchive`/`doDelete`/`doAutotitle`/`onSave*`/`onToggleImmersive`/`onAiReply` 等,经 `hooks/useTavernChatRun.js` 复用 `lib/tavern-chat-run.js` 的收口 run-loop;`MobileRoot.jsx` 引用不变:具名 `export function MobileTavern` + `export default`)+ 文件末 `neededCss` 样式注释;**run 接线宁保守留守,参考 `entries/game-console.jsx` 前例(拆得少=合格)**。自包含展示组件/纯工具纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/tavern/`:`helpers.js`(`relTime`/`tvNow`)、`card.jsx`(`cardFormInit`/`cardFormPayload`/`CardReadout`/`PersonaFields` + 内部 `_cardFields`/`_CARD_MULTILINE`——酒馆 persona 字段集刻意窄于桌面 `pages/cards.jsx`,不复用)、`blocks.jsx`(`MobileToast`/`ToolCallBlock`/`ThinkingBlock`/`Paras`)、`BottomSheet.jsx`(`BottomSheet`——**【防误合点名项】** show 契约一字不改)、`sheets.jsx`(`ChatMenuSheet`/`DeleteConfirmSheet`/`RenameSheet`/`SystemPromptSheet`/`ImportSheet`——**【移动端铁律·防误合】** `DeleteConfirmSheet` 的 `.confirm-preview` 引用框 + 删除钮 trash icon + 包在本地 `BottomSheet`(show 切换 + 滑入)的渲染契约,与桌面/统一 `mobile/Sheet.jsx` 的 `<ConfirmSheet>` open 契约有真实差异,绝不合并/收口,原样保留;细节见 `DeleteConfirmSheet` 上方 GUARD 注释)、`TwoCardDrawer.jsx`(`TwoCardDrawer` 角色卡/persona/系统提示三 tab 右抽屉)、`ListView.jsx`(`ChatListItem`/`ListView`)、`ChatView.jsx`(`ChatView` 对话屏,消息流 + `MobileComposer` + 长按/斜杠/AI 帮回 sheet)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/tavern/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../Composer.jsx`/`../../i18n`/`../../hooks/useStickToBottom.js`/`../../narrative-strip.js`/`../../game-composer.jsx` 全不变);绝不 import 桌面 `tavern-app.jsx`/`tavern-drawer.jsx`。守卫:vitest tavern 测试(`tavern-drawer`/`tavern-thinking-block`/`tavern-tool-inline-order`/`tavern-chat-run`)全部 import 桌面 `tavern-app.jsx`/`tavern-drawer.jsx`/`game-app.jsx`/`lib/tavern-chat-run.js`,不引用本文件 → 拆分零影响;无 Python read_text 守卫引用本文件。
- **[已拆分]** `pages/MobileNewGame.jsx`(440 行壳,原 1345 行)只保留主组件 `MobileNewGame`(向导状态机 + 草稿恢复/回写 `mobile_newgame.draft.v1` + 提交 `handleCreate`(`saves.create`→`updateSettings`→`nav.openGame`)+ 顶栏/底部按钮栏渲染;`MobileRoot.jsx` 引用不变:具名 `export function MobileNewGame` + `export default`);五步向导主体纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/new-game/`:`helpers.js`(常量&工具 `ALLOWED_SOURCES`(出身×身份联动约束,对齐桌面 saves.jsx 语义但**绝不与 `components/saves/` 合并**)/`ORIGIN_OPTIONS`/`STEPS`/`TOTAL_STEPS`/`NEWGAME_*` 状态集/出生点哨兵 `BIRTHPOINT_FROM_START`+`isFromStartBirthpoint`/`scriptBlockReason`)、`shared.jsx`(共用小件 `StepDots`/`ErrBar`/`Loading`/`FieldLabel`——语义统一 #36 点名保留项:`FieldLabel` 只渲染「标签+hint」块,与 `mobile/Field.jsx` 不同形,不收口)、`StepScriptBirth.jsx`(STEP 0 剧本与出生点——**出生点步骤=进度信号病灶 UI,逐字复制,勿改**;「从故事开头开始」必须用户主动点选的哨兵语义 + `birthpointRequired` 上报)、`StepRole.jsx`(STEP 1 角色卡)、`StepIdentity.jsx`(STEP 2 出身与身份:NPC 卡/AI 推荐/手动三来源 + `identityKnown`)、`StepMeta.jsx`(STEP 3 元知识/NPC 起疑/引导强度/防剧透/故事意图)、`StepConfirm.jsx`(STEP 4 确认)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/new-game/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../../i18n`/`../../lib/storage.js` 全不变);**绝不与桌面 `components/saves/`(`NewGameModal`/`BirthpointStep`/`IdentityStep`)合并——移动端自包含实现原样保留**。无 Python/JS 守卫 read_text 引用本文件(`test_models_sync_base_url_override.py` 只拼接 `mobile/settings/`,不受影响)。
- **[已拆分]** `pages/MobileCards.jsx`(65 行壳,原 1492 行)只保留根组件 `MobileCards`(三 Tab user/npc/online 切换,由 `nav.pageId` 定初始 tab;`MobileRoot.jsx` 引用不变:具名 `export function MobileCards` + `export default`,`cards`/`cards-npc`/`cards-online` 三路由同指);页面主体纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/cards/`:`helpers.js`(纯工具/常量 `clamp2`/`clamp3`/`fmtBytes`——**语义统一 #40 点名保留**:`fmtBytes` 空值 → '0 B'、无 GB 档,刻意不用 `window.__fmt.bytes`)、`shared.jsx`(共用小件 `SubHead`/`CardAv`/`Tag`/`ProseBlock`/`Field`/`ScopeSelect`——**语义统一 #36 点名保留**:本地 `Field` 内置 input/textarea 且 desc 用 `<div>`,窄于 `mobile/Field.jsx` 通用 Field,不收口)、`CardForm.jsx`(`CardEditForm` 用户卡+NPC 共用表单)、`CardDetail.jsx`(详情只读面板,信息/设定两 Tab)、`CardEditor.jsx`(编辑器子视图)、`sheets.jsx`(`ImportSheet` 酒馆卡/聊天导入 + `DeleteSheet` 删除确认)、`UserView.jsx`/`NpcView.jsx`/`OnlineView.jsx`(三列表视图,各自持有 list/detail/edit 子状态,与根组件无共享状态 → 整体搬迁不拆闭包)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/cards/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../Field.jsx`/`../../components/AvatarImg.jsx` 全不变);**【移动端铁律】绝不与桌面 `components/cards/` 合并**——唯一跨端 import(`pages/cards.jsx` 的 `cardFormInit`/`cardFormPayload`,字段集与桌面逐字一致)随 `CardEditor` 原样保留,不增不删(仅此一处 desktop 耦合,与原壳同)。`nav.toast` 通路逐字保留;无 Python/JS 守卫 read_text 引用本文件。
- **[已拆分]** `pages/MobileSaves.jsx`(380 行壳,原 1140 行)同型:只保留 `MobileSaves` 路由壳(`saves` 列表/详情 + `saves-branches` 分支树整页两路由,内部 `view` list/detail 切换;`MobileRoot.jsx` 引用不变:具名 `export function MobileSaves` + `export default`)+ 本地 `getSortOpts`/`PAGE_SIZE`(仅壳用);页面主体纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/saves/`:`helpers.js`(纯工具 `API`/`fmtDate`/`normSave`/`normScript`,壳与子文件共用)、`ExportSheet.jsx`(导出 Bundle 底 Sheet)、`SaveSettingsPane.jsx`(存档设置表单)、`BranchListPane.jsx`(分支节点列表)、`SaveDetail.jsx`(详情壳 overview/设置/分支 三 tab,组合前三件 + `ConfirmSheet` 删除/导出;沿用 `mobile/Sheet.jsx` 的 `<ConfirmSheet>` 收口,onClose→onCancel 调用点不变)、`BranchesPage.jsx`(`saves-branches` 分支树整页)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/saves/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../Sheet.jsx` 全不变);绝不 import 桌面 `pages/saves.jsx`/`components/`。无 Python/JS 守卫 read_text 引用本文件。
- **[已拆分]** `pages/MobileCaps.jsx`(92 行壳,原 1135 行)同型:只保留 `MobileCaps` 路由壳(`TABS` 五 section plugins/mcp/skills/apis/feedback 横滑切换 + `nav.page` 同步;`MobileRoot.jsx` 引用不变:具名 `export function MobileCaps` + `export default`);五 section + 共用小件纯机械搬家(逐字节等价、零 DOM/视觉/行为变化)到同级 `mobile/caps/`:`shared.jsx`(共用小件 `Toggle`/`StatusPill`/`MField`——**【移动端铁律·防误合】** `MField` 的 `desc` 用内联 11px/line-height 1.5 `<span>`,与 `mobile/Field.jsx` 的 `.desc`(11.5px/1.55)字号行高不同,刻意保留不合并;细节见 `MField` 上方「语义统一 #36」注释)、`PluginsSection.jsx`(内置插件只读列表)、`McpSection.jsx`(MCP 服务器增删改 + `<Sheet>` 表单)、`SkillsSection.jsx`(可执行 skill + 用户人格 skill 导入)、`ApisSection.jsx`(BYOK 凭证管理)、`FeedbackSection.jsx`(反馈提交/历史 + `CONSENT_TEXT`/`AUP_LINK`/`MAX_FREE_TEXT`/`QQ_*` 常量 + `statusLabel`/`statusColor`/`fmtTime` 本地件)。子文件 import 路径与原壳逐字相同(`mobile/pages/` 与 `mobile/caps/` 同为 `mobile/` 直接子目录 → `../icons.jsx`/`../Sheet.jsx`/`../../lib/crypto-safe.js`/`../../lib/feedback.js` 全不变);Sheet 表单沿用 `mobile/Sheet.jsx` 的 `<Sheet>`(调用点保留 zIndex=70/maxHeight=88%);绝不 import 桌面 `components/`。无 Python/JS 守卫 read_text 引用本文件。

## 测试

- `__tests__/`(vitest 单测,真 EditorView / 真组件)、`e2e/`(Playwright,`playwright.config.js`)。
- 跑法:`cd frontend && npm run build && npm test`。类型:`npm run typecheck`(见 `TYPESCRIPT.md`)。
