/* 转发壳:实现已二次拆分到同目录 —— NewGameModal(主向导 + 就绪判定纯工具)、
   BirthpointStep(Step 3)、IdentityStep(Step 4)。外部只引用 NewGameModal(pages/saves.jsx),
   此壳保持其 import 面不变。 */
export { NewGameModal } from './NewGameModal.jsx';
